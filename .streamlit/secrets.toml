# .streamlit/secrets.toml

GEMINI_API_KEY = "AIzaSyCu_6-IHi9vakKH088SLlUGg9vzrAh_IW8"

# --- For the deployed Streamlit App (Service Account) ---
[gcp_service_account]
type = "service_account"
project_id = "gen-lang-client-0259721154"
# ... (rest of service account keys) ...
client_email = "derma-ai-sheets-editor@gen-lang-client-0259721154.iam.gserviceaccount.com"
# ...

# --- For the local seed_data.py script (OAuth Credentials) ---
# Instructions for this section:
# 1. In Google Cloud Console, go to "APIs & Services" > "Credentials".
# 2. Click "+ CREATE CREDENTIALS" > "OAuth client ID".
# 3. Select "Desktop app" as the application type, give it a name, and click "Create".
# 4. A popup will appear. Click "DOWNLOAD JSON".
# 5. Rename the downloaded file to "desktop_app_credentials.json" and place it in the project's root directory.
# This file should NOT be in secrets.toml, but exist alongside it. The seed script will use it.
# It is gitignored by default in most Python .gitignore files.
